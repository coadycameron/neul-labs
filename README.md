# Neul Labs Rocket Site

A dependency-free, scroll-driven landing page built with HTML, CSS, and JavaScript.

## Run locally

```bash
npm start
```

Then open [http://localhost:4173](http://localhost:4173).

The contact form is a front-end demo. Connect the submit handler in `script.js` to your preferred form or CRM endpoint for production.
